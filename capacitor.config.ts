import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Planeta-verde-v.1.1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
